---
title: Demo
description: // TODO(HiDeoo)
pagefind: false
---

This page contains various `diff` code blocks gathered from the [Astro](https://astro.build/) and [Starlight](https://starlight.astro.build/) documentations to compare the differences between the classic `diff` syntax and the one used with the `remark-auto-diff` plugin.

## `package.json` example

import { Tabs, TabItem } from '@astrojs/starlight/components'
import { Preview } from '@hideoo/starlight-plugins-docs-components'

<Tabs syncKey="remark-auto-diff-demo">

<TabItem label="Classic diff">

<Preview>

````md frame="terminal" title="Markdown Code Editor"
```diff title="package.json"
{
  "scripts": {
-    "test": "echo \"Error: no test specified\" && exit 1"
+    "dev": "astro dev",
+    "build": "astro build",
+    "preview": "astro preview"
  }
}
```
````

<Fragment slot="preview">

```diff title="package.json"
{
  "scripts": {
-    "test": "echo \"Error: no test specified\" && exit 1"
+    "dev": "astro dev",
+    "build": "astro build",
+    "preview": "astro preview"
  }
}
```

</Fragment>

</Preview>

</TabItem>

<TabItem label="remark-auto-diff">

<Preview>

````md frame="terminal" title="Markdown Code Editor"
```json auto-diff title="package.json"
{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
```

```json auto-diff
{
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  }
}
```
````

<Fragment slot="preview">

```json auto-diff title="package.json"
{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
```

```json auto-diff
{
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  }
}
```

</Fragment>

</Preview>

</TabItem>

</Tabs>

## `astro.config.mjs` example

<Tabs syncKey="remark-auto-diff-demo">

<TabItem label="Classic diff">

<Preview>

````md frame="terminal" title="Markdown Code Editor"
```diff
// astro.config.mjs
import { defineConfig } from 'astro/config'
import starlight from '@astrojs/starlight'

export default defineConfig({
  integrations: [
    starlight({
      title: 'Docs With Custom CSS',
-      customCss: [],
+      customCss: [
+        // Relative path to your custom CSS file
+        './src/styles/custom.css',
+      ],
    }),
  ],
})
```
````

<Fragment slot="preview">

```diff
// astro.config.mjs
import { defineConfig } from 'astro/config'
import starlight from '@astrojs/starlight'

export default defineConfig({
  integrations: [
    starlight({
      title: 'Docs With Custom CSS',
-      customCss: [],
+      customCss: [
+        // Relative path to your custom CSS file
+        './src/styles/custom.css',
+      ],
    }),
  ],
})
```

</Fragment>

</Preview>

</TabItem>

<TabItem label="remark-auto-diff">

<Preview>

````md frame="terminal" title="Markdown Code Editor"
```js auto-diff
// astro.config.mjs
import { defineConfig } from 'astro/config'
import starlight from '@astrojs/starlight'

export default defineConfig({
  integrations: [
    starlight({
      title: 'Docs With Custom CSS',
      customCss: [],
    }),
  ],
})
```

```js auto-diff
// astro.config.mjs
import { defineConfig } from 'astro/config'
import starlight from '@astrojs/starlight'

export default defineConfig({
  integrations: [
    starlight({
      title: 'Docs With Custom CSS',
      customCss: [
        // Relative path to your custom CSS file
        './src/styles/custom.css',
      ],
    }),
  ],
})
```
````

<Fragment slot="preview">

```js auto-diff
// astro.config.mjs
import { defineConfig } from 'astro/config'
import starlight from '@astrojs/starlight'

export default defineConfig({
  integrations: [
    starlight({
      title: 'Docs With Custom CSS',
      customCss: [],
    }),
  ],
})
```

```js auto-diff
// astro.config.mjs
import { defineConfig } from 'astro/config'
import starlight from '@astrojs/starlight'

export default defineConfig({
  integrations: [
    starlight({
      title: 'Docs With Custom CSS',
      customCss: [
        // Relative path to your custom CSS file
        './src/styles/custom.css',
      ],
    }),
  ],
})
```

</Fragment>

</Preview>

</TabItem>

</Tabs>
